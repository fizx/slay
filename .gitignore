# Compiled artifacts
target/

# IDE - VSCode
.vscode/

# These are backup files generated by rustfmt
**/*.rs.bk

# Cargo.lock if using cargo workspaces
# Uncomment the line below if you have a Cargo workspace and don't want to commit the lockfile for the root crate
# Cargo.lock
